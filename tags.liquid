---
title: Category Cloud
layout: main
---

<h3>Tag Cloud</h3>

<div class="tag-cloud">
  {% for cat in collections.tagCloud %}
    <a 
      href="/categories/{{ cat.slug }}/" 
      class="tag-cloud-item" 
      style="font-size: {{ cat.fontSize }};"
    >
      {{ cat.name }} 
      <span class="tag-count">({{ cat.count }})</span>
    </a>
  {% endfor %}
</div>