---
title: Blog
layout: main
---

{% for post in collections.post reversed %}
<p>
<a href="{{post.url}}">{{ post.data.title }}</a> <div class="post-date">{{ post.date | niceDate }}</div><br/>
{% excerpt post %}
</p>
{% endfor %}